<%= form_with(model: every_pay) do |form| %>
  <% if every_pay.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(every_pay.errors.count, "error") %> prohibited this every_pay from being saved:</h2>

      <ul>
        <% every_pay.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :status %>
    <%= form.text_field :status %>
  </div>

  <div class="field">
    <%= form.label :amount %>
    <%= form.text_field :amount %>
  </div>

  <div class="field">
    <%= form.label :response %>
    <%= form.text_field :response %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
